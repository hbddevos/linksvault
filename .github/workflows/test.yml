# name: Tests

# on:
#   push:
#     branches:
#       - develop
#       - main
#       - master
#   pull_request:
#     branches:
#       - develop
#       - main
#       - master

# permissions:
#   contents: read
#   checks: write

# jobs:
#   tests:
#     name: Run Tests
#     runs-on: ubuntu-latest
    
#     services:
#       mysql:
#         image: mysql:8.0
#         env:
#           MYSQL_ROOT_PASSWORD: root
#           MYSQL_DATABASE: ribedu_test
#         ports:
#           - 3306:3306
#         options: >-
#           --health-cmd="mysqladmin ping"
#           --health-interval=10s
#           --health-timeout=5s
#           --health-retries=3

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup PHP
#         uses: shivammathur/setup-php@v2
#         with:
#           php-version: '8.4'
#           extensions: mbstring, pdo, pdo_mysql, bcmath, mysql
#           coverage: none

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'

#       - name: Get Composer Cache Directory
#         id: composer-cache
#         run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

#       - name: Cache Composer dependencies
#         uses: actions/cache@v4
#         with:
#           path: ${{ steps.composer-cache.outputs.dir }}
#           key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
#           restore-keys: |
#             ${{ runner.os }}-composer-

#       - name: Cache NPM dependencies
#         uses: actions/cache@v4
#         with:
#           path: ~/.npm
#           key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
#           restore-keys: |
#             ${{ runner.os }}-npm-

#       - name: Install Composer dependencies
#         run: composer install --no-interaction --prefer-dist --optimize-autoloader

#       - name: Install NPM dependencies
#         run: npm ci

#       - name: Generate application key
#         run: php artisan key:generate

#       - name: Create database for testing
#         run: |
#           mysql -h 127.0.0.1 -u root -proot -e "CREATE DATABASE IF NOT EXISTS ribedu_test;"

#       - name: Run database migrations
#         env:
#           DB_CONNECTION: mysql
#           DB_HOST: 127.0.0.1
#           DB_PORT: 3306
#           DB_DATABASE: ribedu_test
#           DB_USERNAME: root
#           DB_PASSWORD: root
#         run: php artisan migrate --force

#       - name: Build assets
#         run: npm run build

#       - name: Run tests with coverage
#         env:
#           DB_CONNECTION: mysql
#           DB_HOST: 127.0.0.1
#           DB_PORT: 3306
#           DB_DATABASE: ribedu_test
#           DB_USERNAME: root
#           DB_PASSWORD: root
#           APP_ENV: testing
#           CACHE_DRIVER: array
#           SESSION_DRIVER: array
#           QUEUE_CONNECTION: sync
#         run: php artisan test --coverage

#       - name: Upload test results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: test-results
#           path: |
#             phpunit.xml
#             tests/
#           retention-days: 30
